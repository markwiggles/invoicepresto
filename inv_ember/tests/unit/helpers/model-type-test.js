import {
  modelType
} from 'inv-ember/helpers/model-type';

module('ModelTypeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = modelType(42);
  ok(result);
});
